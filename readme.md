# dist related notes

## to run the test launch
- make sure the local MySQL is running
  - System Settings > (left panel scroll all the way down) > MySQL > start
- $ ./test_launch
- it will start 2 servers
  - localhost:60193 is the web site
  - localhost:3000 is the backend service

## to push the site to soaringwild.org